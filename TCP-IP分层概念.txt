# 2019-1-10 14:15:42
Title: TCP/IP分层概念
4 -> 3 -> 2 -> 1
1.应用层: 
    HTTP, FTP...
    为应用程序提供服务。

2.传输层：
   TCP、UDP
   可靠的传输作用;只在通信双方节点上处理，而无须路由器处理。

3.网络层：
   IP
   将数据传输到目标地址;主要负责寻址和路由选择。


4.物理链路层：
   负责物理层面上的互连的，节点之间的传输。
