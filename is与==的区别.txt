# 2018-11-8 11:13:47
is   判断两个变量是否同一引用对象
= =  判断引用变量两者的值是否相等

例如：
   a = [1, 2, 3]
   # 同一引用，相同值
   b = a 
   b is a: True
   b == a: True
   # 不同引用，相同值
   b = a[:]
   b is a: Flase
   b == a: True
   
   